# server {
#     listen 80;
#     server_name sepaywebhook.5kaquarium.com;

#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
# }

# server {
#     listen 443 ssl;
#     server_name zalowebhookdev.5kaquarium.com;

#     ssl_certificate /etc/letsencrypt/live/zalowebhookdev.5kaquarium.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/zalowebhookdev.5kaquarium.com/privkey.pem;

#     location /zalo/webhook/ {
#         proxy_pass http://zalowebhookdev_container:5001/api/service_zalo_webhook/zalo/webhook;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }

#     location /zalo/tokenCallback/ {
#         proxy_pass http://zalowebhookdev_container:5001/api/service_zalo_webhook/zalo/tokenCallback;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }

# server {
#     listen 443 ssl;
#     server_name sepaywebhookdev.5kaquarium.com;

#     ssl_certificate /etc/letsencrypt/live/sepaywebhookdev.5kaquarium.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/sepaywebhookdev.5kaquarium.com/privkey.pem;

#     location /sepay-webhook {
#         proxy_pass http://sepaywebhookdev_container:7001/service_webhook/mutate/sepay-webhook;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }

upstream backend {
    server nodeserver_container:4000 max_fails=3 fail_timeout=30s;
    server nodeserver1_container:4001 max_fails=3 fail_timeout=30s;
    server nodeserver2_container:4002 max_fails=3 fail_timeout=30s;
}
server {
    listen 443 ssl;
    server_name api.taokosao.com;
    client_max_body_size 100M;

    ssl_certificate /etc/letsencrypt/live/api.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.taokosao.com/privkey.pem;

    location / {
        proxy_pass http://backend;

        proxy_connect_timeout 3s;
        proxy_read_timeout 30s;

        proxy_next_upstream error timeout http_502 http_503 http_504;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # location /service_image_v1/ {
    #     proxy_pass http://nodeserver_container:4000/service_image_v1/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }

    # location /service_video_v1/ {
    #     proxy_pass http://nodeserver_container:4000/service_video_v1/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }
}

server {
    listen 443 ssl;
    server_name zalowebhook.taokosao.com;

    ssl_certificate /etc/letsencrypt/live/zalowebhook.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zalowebhook.taokosao.com/privkey.pem;

    location /zalo/webhook/ {
        proxy_pass http://zalowebhook_container:5000/api/service_zalo_webhook/zalo/webhook;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /zalo/tokenCallback/ {
        proxy_pass http://zalowebhook_container:5000/api/service_zalo_webhook/zalo/tokenCallback;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 443 ssl;
    server_name sepaywebhook.taokosao.com;

    ssl_certificate /etc/letsencrypt/live/sepaywebhook.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sepaywebhook.taokosao.com/privkey.pem;

    location /sepay-webhook {
        proxy_pass http://sepaywebhook_container:7000/service_webhook/mutate/sepay-webhook;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 443 ssl;
    server_name socket.taokosao.com;

    ssl_certificate /etc/letsencrypt/live/socket.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/socket.taokosao.com/privkey.pem;

    set $cors_origin "";

    if ($http_origin ~* ^https://(chat\.taokosao\.com|admin\.taokosao\.com|app\.taokosao\.com)$) {
        set $cors_origin $http_origin;
    }

    location /socket.io/ {

        # OPTIONS
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            add_header Content-Type text/plain;
            return 204;
        }

        add_header Access-Control-Allow-Origin $cors_origin always;
        add_header Access-Control-Allow-Credentials "true" always;

        proxy_pass http://socket_container:6000;

        # QUAN TRỌNG CHO WEBSOCKET
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_read_timeout 86400;
    }
}

server {
    listen 443 ssl;
    server_name ztks.taokosao.com;

    ssl_certificate /etc/letsencrypt/live/ztks.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ztks.taokosao.com/privkey.pem;

    # Proxy tất cả request về webapp_container (serving static)
    location / {
        proxy_pass http://chatapp_container:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection 'upgrade';
        # proxy_set_header Host $host;
        # proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 443 ssl;
    server_name wztks.taokosao.com;

    ssl_certificate /etc/letsencrypt/live/wztks.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wztks.taokosao.com/privkey.pem;

    location / {
        proxy_pass http://webapp_container:3100;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection 'upgrade';
        # proxy_set_header Host $host;
        # proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 443 ssl;
    server_name mtks.taokosao.com;

    ssl_certificate /etc/letsencrypt/live/mtks.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mtks.taokosao.com/privkey.pem;

    location / {
        proxy_pass http://adminapp_container:3200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection 'upgrade';
        # proxy_set_header Host $host;
        # proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 443 ssl;
    server_name zalowebhookdev.taokosao.com;

    ssl_certificate /etc/letsencrypt/live/zalowebhookdev.taokosao.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zalowebhookdev.taokosao.com/privkey.pem;

    location /zalo/webhook/ {
        proxy_pass http://zalowebhookdev_container:5000/api/service_zalo_webhook/zalo/webhook;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /zalo/tokenCallback/ {
        proxy_pass http://zalowebhookdev_container:5000/api/service_zalo_webhook/zalo/tokenCallback;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}